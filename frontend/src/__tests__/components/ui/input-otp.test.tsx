import { render } from '@testing-library/react';
import input-otp from '@/components/ui/input-otp';

describe('Component: input-otp', () => {
  it('renders without crashing', () => {
    render(<input-otp />);
  });
});
