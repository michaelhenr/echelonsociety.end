import { render } from '@testing-library/react';
import sonner from '@/components/ui/sonner';

describe('Component: sonner', () => {
  it('renders without crashing', () => {
    render(<sonner />);
  });
});
