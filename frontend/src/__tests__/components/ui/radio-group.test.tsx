import { render } from '@testing-library/react';
import radio-group from '@/components/ui/radio-group';

describe('Component: radio-group', () => {
  it('renders without crashing', () => {
    render(<radio-group />);
  });
});
