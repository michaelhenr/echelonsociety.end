import { render } from '@testing-library/react';
import aspect-ratio from '@/components/ui/aspect-ratio';

describe('Component: aspect-ratio', () => {
  it('renders without crashing', () => {
    render(<aspect-ratio />);
  });
});
